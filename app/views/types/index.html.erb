<h1>Tipos de creaciones</h1>
<!--
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
-->
  <tbody>
    <% @types.each do |type| %>
      <tr>
<!--        <td><a href="type/<%=type.id%>/items"><%= type.name %></a></td>
        <td><%= link_to 'Show', type %></td>
        <td><%= link_to 'Edit', edit_type_path(type) %></td>
        <td><%= link_to 'Destroy', type, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        <%= render :partial => "type", :locals => {:type => type} %>
<!--         <td><%= type.description %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Type', new_type_path %>
